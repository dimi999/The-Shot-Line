<script>
    var tema = localStorage.getItem("tema");
    if(tema)
        document.body.classList.add("dark");
    else
        document.body.classList.remove("dark");
</script>
<header>
    <h1>The Shot Line</h1>
    <q>Success is not an accident, success is actually a choice</q>

    <nav>
        <label id="hamburger" for="ch-menu">
            <img src="Resurse/imagini/hamburger-menu.png" alt="menu">
        </label>
        <input id="ch-menu" type="checkbox">
        <ul class="meniu">
            <li>
                <a href="/index">&#127968 <span class="acasa">Home</span></a>
                <ul class="submeniu">
                    <li><a href="/index#details">Our offer</a></li>
                    <li><a href="/index#shipment">Shipment</a></li>
                </ul>
            </li>
            <li><a href="/despre">About Us</a></li>
            <li><a href="/store">Store</a>
                <ul class="submeniu">
                    <%
                        for(let opt of locals.categs) {
                            let crt = opt.unnest, str = "/store?type=" + crt;
                            console.log(crt); %>
                        <li><a href=<%- str %>><%- crt %></a></li>    
                    <% } %>
                </ul>
            </li>
            <li><a href="/pagina3.html">Contact Us</a></li>
            <% if(!locals.utilizator) { %>
            <li><a href="/register">Register</a></li>
            <% } else { %>
            <li><a href="/profil">Profil</a></li>
            <% } %>    
        </ul>
    </nav>

    <% 
        console.log(locals.utilizator)
        if(!locals.utilizator) {
    %>
    <form id="form_log"  method="post" class="date"  action="/login"  enctype="multipart/form-data">
        <p>
            <label>
                Username: <input type="text" name="username" id="inp-usernamelog" value="" required pattern="[A-Za-z0-9]+">
            </label>
        </p>
        <p>
            <label>
                Password: <input id="parllog" type="password" required name="parola" value="">
            </label>
        </p>
        <p><input type="submit" value="Login"></p>
    </form>
    <% }
    else { %>
        <div id="salutare">
            <p>Salut <%- locals.utilizator.username %>!</p>
            <a href="/logout"><button id="logout" >Logout</button></a>
        </div>
    <% } %>
    <p><button id="btn_tema"><i class="fa-solid fa-sun"></i></button></p>
</header>
